# 《时间裂隙》AI Studio 辅助开发 Prompt 脚本

> **使用说明**：
> 1. 请将以下 Prompt 按顺序（Phase 1 -> Phase 5）逐个复制并粘贴到 Google AI Studio 中。
> 2. 每次生成后，请先在本地运行测试，确认无误后再进行下一阶段的生成。
> 3. 如果 AI 生成的代码有遗漏或错误，请在当前对话中要求其修正，不要急于进入下一阶段。

---

## Phase 1: 核心骨架 (基础增量循环)

**请复制以下内容并发送给 AI Studio：**

```text
你现在是一个资深的 Phaser.js 游戏开发专家。我需要你帮我编写一个名为《时间裂隙》的科幻增量游戏 Demo。

请使用 Phaser 3 (v3.80+) 编写游戏的基础场景 `MainScene`。

**核心需求：**
1. **基础资源**：包含一个名为 `chronon` (时间能量) 的资源，初始为 0，每秒自动增长。
2. **生产器系统**：实现 5 个级别的“时间装置”（生产器）。
   - G1: 粒子加速器 (Base Cost: 10, Scale: 1.15, Base Prod: 1/s)
   - G2: 时间弦引擎 (Base Cost: 100, Scale: 1.16, Base Prod: 8/s)
   - G3: 因果编辑器 (Base Cost: 1200, Scale: 1.17, Base Prod: 55/s)
   - G4: 宇宙时钟 (Base Cost: 15000, Scale: 1.18, Base Prod: 350/s)
   - G5: 奇点编译器 (Base Cost: 220000, Scale: 1.20, Base Prod: 2100/s)
3. **经济公式**：
   - 购买成本 = Base Cost * (Scale ^ 当前拥有数量)
   - 总产出 (ChrononPerSecond) = Σ(拥有数量 * Base Prod)
4. **UI 展示**：
   - 顶部居中显示当前 `chronon` 数量和每秒产出速率 (Chronon/s)。
   - 界面中提供 5 个按钮用于购买这 5 种装置，按钮上需显示装置名称、当前拥有数量、当前购买成本。
   - 当 `chronon` 不足时，购买按钮应呈现不可点击状态（如变灰）。
5. **技术要求**：
   - 使用 `requestAnimationFrame` 下的 `deltaTime` 进行 Tick 计算，确保产出平滑。
   - 代码结构需模块化，尽量将经济计算逻辑 (`EconomySystem`) 与 UI 渲染逻辑分离。

请提供完整的、可直接在浏览器中运行的 `index.html` 和 `main.js` 代码。
```

---

## Phase 2: 经济深化与 UI 布局优化

**请复制以下内容并发送给 AI Studio：**

```text
基于上一轮生成的代码，我们需要深化经济系统并优化 UI 布局。

**核心需求：**
1. **中心辐射式 UI 布局**：
   - 放弃简单的列表布局。
   - 在屏幕中心放置一个“中心核 (Core)”，用于显示当前的 `chronon` 数量。
   - 将 5 个时间装置的购买按钮以环形 (Ring) 均匀分布在中心核的周围。
2. **全局倍率 (Global Multiplier)**：
   - 引入一个全局产出倍率变量 `globalMultiplier`，初始值为 1。
   - 修改总产出公式：`ChrononPerSecond = (Σ(拥有数量 * Base Prod)) * globalMultiplier`。
3. **基础升级 (Upgrades)**：
   - 在界面下方或侧边添加一个“升级”面板。
   - 实现 3 个基础升级项（例如：提升 G1 产出 100%、全局产出提升 50%、降低所有装置成本 10%）。
   - 升级项只能购买一次，购买后应用相应的效果并从面板中移除或标记为已购买。
4. **数值格式化**：
   - 当 `chronon` 或成本数值过大（如超过 1,000,000）时，使用科学计数法或字母后缀（如 1M, 1B, 1T）进行格式化显示。

请提供更新后的完整代码，确保 UI 布局美观且具有科幻感（可以使用深色背景和发光颜色的文本/图形）。
```

---

## Phase 3: 双层 Prestige 系统

**请复制以下内容并发送给 AI Studio：**

```text
现在，我们需要为游戏引入双层 Prestige（重置/声望）系统。

**核心需求：**
1. **第一层重置 (P1) - 时间回溯**：
   - 引入新货币 `Time Crystal` (TC)。
   - P1 收益公式：`TC Gain = floor((LifetimeChronon / 10^6)^0.5)`。(`LifetimeChronon` 是历史累计获得的总 `chronon`)。
   - 在 UI 外环添加“时间回溯”按钮，按钮上需实时显示当前重置可获得的 TC 数量。
   - 点击执行 P1：重置当前的 `chronon`、所有装置数量、基础升级进度；保留 TC 数量和 `LifetimeChronon`。
2. **第二层重置 (P2) - 平行宇宙跃迁**：
   - 引入新货币 `Dimension Shard` (DS)。
   - P2 收益公式：`DS Gain = floor((LifetimeCrystal / 10^4)^0.6)`。(`LifetimeCrystal` 是历史累计获得的总 TC)。
   - 在 UI 外环添加“平行宇宙跃迁”按钮，实时显示可获得的 DS 数量。
   - 点击执行 P2：重置 `chronon`、装置、基础升级、TC 数量以及 P1 科技进度；保留 DS 数量和 `LifetimeCrystal`。
3. **Prestige 科技树 (简化版)**：
   - 提供一个简单的面板，允许玩家消耗 TC 购买 P1 科技（例如：全局产出 +10%）。
   - 提供另一个面板，允许玩家消耗 DS 购买 P2 科技（例如：P1 收益 +5%）。
4. **代码架构**：
   - 强烈建议将 Prestige 相关的计算和重置逻辑封装到一个独立的 `PrestigeSystem` 类或模块中，避免 `MainScene` 过于臃肿。

请提供更新后的代码，并确保重置时的数据边界清晰无误。
```

---

## Phase 4: Roguelite 副本 (时间裂隙)

**请复制以下内容并发送给 AI Studio：**

```text
接下来是游戏的核心特色：独立的 Roguelite 副本系统。

**核心需求：**
1. **副本场景 (DungeonScene)**：
   - 创建一个新的 Phaser 场景 `DungeonScene`。
   - 在主场景 (`MainScene`) 提供一个“进入时间裂隙”的入口按钮。
   - 副本拥有独立的 120 秒倒计时。
   - 副本内有独立的 `RunScore`（初始为 0），随时间或点击增长（模拟局内产出）。
2. **局内构筑 (Drafting)**：
   - 副本进行中，每 15 秒暂停一次倒计时，弹出一个 3 选 1 的 Buff 选择界面。
   - 设计 5-6 种不同的 Buff（例如：产出翻倍、倒计时延长 10 秒、每次点击得分增加等）。
   - 玩家选择一个 Buff 后，立即应用其效果，并恢复倒计时。
3. **标签组合 (Synergy)**：
   - 每个 Buff 附带一个标签（如 `QUANTUM`, `CAUSAL`）。
   - 统计玩家已选 Buff 的标签数量。当某个标签达到 2 个时，触发额外的 Synergy 效果（例如：`QUANTUM` x2 = 全局得分额外 +20%）。
4. **结算与返回**：
   - 120 秒倒计时结束，副本结算。
   - 结算公式：获得 `Anomaly Data` (AD) = `floor((RunScore / 10^5)^0.65)`。
   - 显示结算界面，展示最终得分和获得的 AD，然后返回 `MainScene`。
   - 在 `MainScene` 中，AD 可用于购买局外永久科技（如：副本初始时间 +10 秒）。

请提供包含 `MainScene` 和 `DungeonScene` 切换逻辑的完整代码。
```

---

## Phase 5: 视觉动效与本地持久化

**请复制以下内容并发送给 AI Studio：**

```text
最后，我们需要打磨游戏的视觉体验并加入存档功能。

**核心需求：**
1. **科幻动效 (Phaser Particles & Tweens)**：
   - **资源飞入**：当玩家购买时间装置时，生成粒子特效，从装置节点沿曲线飞入中心核。请使用 Phaser 的粒子对象池以保证性能。
   - **数值平滑滚动**：当 `chronon` 数量发生较大变化（如购买昂贵升级或重置后），中心核的数值显示应使用 Tween 进行平滑插值滚动，而不是瞬间跳变。
   - **脉冲反馈**：当 `chronon` 达到某些里程碑（如 10k, 1M）时，中心核触发一次 Additive Blend 的发光脉冲特效。
   - **副本氛围**：在 `DungeonScene` 中，当倒计时剩余最后 20 秒时，让背景颜色发生渐变（如转为暗红色），并添加轻微的屏幕边缘闪烁（Vignette 效果），营造紧张感。
2. **本地存档 (SaveSystem)**：
   - 实现一个 `SaveSystem`，利用 `localStorage` 每隔 10 秒自动保存当前游戏状态（包括所有货币、装置数量、升级进度、Prestige 状态等）。
   - 游戏启动时自动读取存档。
3. **离线收益**：
   - 在存档中记录最后一次保存的时间戳 (`lastTimestamp`)。
   - 游戏加载时，计算当前时间与 `lastTimestamp` 的差值（离线时间）。
   - 离线收益 = `离线时间(秒) * 离线前的 ChrononPerSecond * 0.7`（最高计算 8 小时）。
   - 登录时弹窗提示玩家获得了多少离线收益。

请在不破坏现有经济和副本逻辑的前提下，整合这些视觉特效和存档功能，提供最终版本的完整代码。
```